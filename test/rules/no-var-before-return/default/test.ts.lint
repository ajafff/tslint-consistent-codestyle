function foo(bar) {
    let baz = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}

function foo(bar) {
    let baz = bar, foo = baz;
    return baz;
}

function foo(bar) {
    let baz = bar, foo = ajaff(bar);
    return baz;
}

function foo(bar) {
    let {baz} = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}

function foo(bar) {
    let {foo: baz} = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}

function foo(bar) {
    let {foo: {bar: baz}} = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}

function foo(bar) {
    let [baz] = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}

function foo(bar) {
    let [[baz], bas] = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}

function foo(bar) {
    let [{bar: [baz]}] = bar;
    return baz;
           ~~~  [don't declare a variable to return it immediately]
}